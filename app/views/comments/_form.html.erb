<%= form_with model:[review, comment], url: review_comments_path(review_id: review.id), action: :post do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに" %>
  <%= f.submit "送信する" %>
<% end %>