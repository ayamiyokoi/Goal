<%= form_with model: [group, chat], url: group_chats_path(group_id: group.id), action: :post do |f| %>
  <%= f.text_field :chat, placeholder: "メッセージを入力して下さい" , size: 70, class:"form-text-field" %>
  <%= f.submit "投稿", class: 'form-submit' %>
<% end %>