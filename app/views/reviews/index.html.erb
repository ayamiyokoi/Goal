<p id="notice"><%= notice %></p>

<h1>Reviews</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Tag</th>
      <th>Rate</th>
      <th>Review</th>
      <th>Today</th>
      <th>Plan</th>
      <th>Tomorrow</th>
      <th>Title</th>
      <th>Topic</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reviews.each do |review| %>
      <tr>
        <td><%= review.user_id %></td>
        <td><%= review.tag_id %></td>
        <td><%= review.rate %></td>
        <td><%= review.review %></td>
        <td><%= review.today_id %></td>
        <td><%= review.plan %></td>
        <td><%= review.tomorrow_id %></td>
        <td><%= review.title %></td>
        <td><%= review.topic %></td>
        <td><%= link_to 'Show', review %></td>
        <td><%= link_to 'Edit', edit_review_path(review) %></td>
        <td><%= link_to 'Destroy', review, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Review', new_review_path %>
